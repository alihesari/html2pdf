import React from "react";

import "./Logo.scss";

const logo = (porps) => {
    return (
        <a href="./" className="navbar-logo">
            <img src="./logo.png" />
        </a>
    );
};

export default logo;
